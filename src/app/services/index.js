import auth from './auth';
import firestore from './firestore';
import http from './http';
import storage from './storage';

export { auth, firestore, http, storage };

export default {
  auth,
  firestore,
  http,
  storage,
};
